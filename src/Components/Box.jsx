import React from 'react';

const Box = (props) => {
    return (
        <div className='box' /*style={{ width: `${100 / props.number}% `}}*/ >
            box
        </div>
    );
}

export default Box;
